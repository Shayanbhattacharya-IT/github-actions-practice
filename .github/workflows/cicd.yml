name: CICD

on:
  workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: Clone the code
        run: echo "cloning the code"
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build using Docker
        run: echo "build using Docker"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Running the test
        run: echo "test the code"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploying on the machine
        run: echo "deploying the code"
